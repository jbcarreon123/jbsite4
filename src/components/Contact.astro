<script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer
></script>

<form action="https://contact-form.jbcarreon212.workers.dev/" method="POST">
    <div class="form_1">
        <input
            type="text"
            placeholder="name *"
            name="name"
            required
            aria-label="your name (required)"
        />
        <input
            type="text"
            placeholder="email *"
            name="email"
            required
            aria-label="your email (required)"
        />
        <input
            type="text"
            placeholder="website"
            name="website"
            aria-label="your website (optional)"
        />
    </div>

    <textarea
        placeholder="your query *"
        name="message"
        required
        aria-label="your query (required)"></textarea>

    <div class="form_2">
        <div>
            <p id="req"><i>* required</i></p>
            <div class="cf-turnstile" data-sitekey="0x4AAAAAABwXs7oYZ9A7qMFy"></div>
        </div>
        <button type="submit"
            ><span aria-hidden="true" class="ms" data-icon="send"></span> Send!</button
        >
    </div>
</form>

<style slot="head">
    input,
    textarea,
    button {
        padding: 6px;
        background: var(--altbg);
        text: var(--text);
    }

    button {
        height: fit-content;
    }

    .form_1 {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 6px;
        padding-bottom: 6px;
    }

    .form_1 input {
        width: 100%;
    }

    .form_2 {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        padding-top: 6px;
    }

    textarea {
        width: 100%;
        height: 200px;
    }

    @media screen and (width <= 640px) {
        .form_1 {
            grid-template-columns: repeat(1, 100%);
        }
    }
</style>
